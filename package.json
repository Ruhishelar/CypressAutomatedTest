{
  "name": "cypresseveningbatch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "smoke":" cypress run --spec ./cypress/e2e/first.cy.js --headed",
    "regression":" cypress run --spec ./cypress/e2e/registration.cy.js",
    "e2e":" cypress run",
    "chrome":" cypress run --spec ./cypress/e2e/login.cy.js --browser=chrome --headed"
    
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.6.0",
    "cypress-file-upload": "^5.0.8",
    "cypress-mochawesome-reporter": "^3.2.2",
    "cypress-xpath": "^2.0.1"
  },
  "dependencies": {
    "cypress-real-events": "^1.7.1"
  }
  
}
